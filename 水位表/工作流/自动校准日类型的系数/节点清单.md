# 节点清单

| 顺序 | 节点名称 | 节点 Key | 主要读取 | 主要写入 | 文件 |
|---:|---|---|---|---|---|
| 1 | 自动计算活动系数 | `1bbaijfre5q` | `daily_sales`、`datetypetime`、`sales_coefficient` | `sales_coefficient.coefficient` | [02-自动计算活动系数.sql](02-自动计算活动系数.sql) |
| 2 | 同步系数 | `rbqovmyvse9` | `daily_sales`、`sales_coefficient` | `daily_sales.coefficient` | [03-同步未来日类型系数.sql](03-同步未来日类型系数.sql) |
| 3 | 更新预计销量 | `9uyp07gvgoi` | `daily_sales.weighted_sales`、`daily_sales.coefficient` | `daily_sales.maybe_sales` | [04-更新预计销量.sql](04-更新预计销量.sql) |

节点按线上当前版本的 `upstreamId`、`downstreamId` 链路排序。

`02-自动计算活动系数.sql` 已在线上使用只计算“基础活动类型”的新规则，本地文件与线上当前节点一致。

## 待重新加入的恢复节点 SQL

| 文件 | 用途 | 当前线上状态 |
|---|---|---|
| [01-非自动计算类型恢复初始系数.sql](01-非自动计算类型恢复初始系数.sql) | 不使用 `mark`，在每次运行时将非基础活动类型恢复为 `datetypetime` 中唯一匹配的初始系数 | 对应节点已删除 |
