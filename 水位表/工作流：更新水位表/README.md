# 工作流：更新水位表

- 工作流 Key：`75uyibjgtr3`
- 来源文件：`C:\Users\Aniya\Desktop\新建文本文档.txt`
- 整理日期：2026-07-10
- 节点数量：9 个 SQL 节点

## 文件说明

- `00-原始复制内容.txt`：未经改写的原始复制内容，作为核对基线。
- `01` 至 `09`：按执行顺序拆分的 SQL 节点代码。
- `06-生成合计记录-单量更新.sql`：节点 6 的单量版，通过 `:asin`、`:country` 只处理指定商品和国家。
- `节点清单.md`：节点顺序、完整名称和对应文件。

## 使用方式

后续可指定某个 `.sql` 文件让 Codex 分析或修改。修改前应同时检查该节点的上游输入、下游写回和相关字段，避免改变既有计算口径。

## 当前资料边界

本次来源是人工复制的节点代码，仅能确认 9 个 SQL 节点及其顺序。触发器配置、节点数据库 ID、节点 Key、启用状态和版本信息未包含在源文件中，因此未作推测或补写。

## 本地修改状态

`06-生成合计记录.sql` 已调整为昨日销量延迟兼容模式：昨天仅回补销量和基准销量；系数、型号、类型及加权基准销量只更新今天及未来；前天及更早保持不变。

`06-生成合计记录-单量更新.sql` 保留上述计算和日期保护口径，并在店铺明细、已有合计行、基础库存及最早日期子查询中同时增加 `asin = :asin`、`country = :country` 条件。参数缺失时不会自动退回全量版本。

该修改目前仅存在于本地文件，尚未更新、启用或执行线上 NocoBase 工作流。原始复制内容仍保存在 `00-原始复制内容.txt`，可作为回滚基线。

## 历史矫正包

- `10-历史加权基准销量矫正预览.sql`：只读复算并列出当前候选记录。
- `历史加权基准销量矫正候选.csv`：2026-07-10 生成的 710 条候选快照，包含主键、旧值、新值和差异。
- `11-定向矫正历史加权基准销量.sql`：仅更新候选合计行的 `weighted_sales`，并校验数据库旧值仍与快照一致。
- `12-回滚历史加权基准销量矫正.sql`：仅在当前值仍等于本次新值时恢复旧值。

历史加权基准销量已于 2026-07-10 通过逐条主键更新完成，共更新并验证 710 条；执行结果保存在 `历史加权基准销量矫正执行结果.csv`。独立复核另发现 33 条不在本次快照内的历史差异记录，暂未继续写入。回滚 SQL 仍可用于恢复本次旧值。
